import { ProfileClient } from "./ProfileClient";

export const metadata = {
  title: "Profile",
  description: "Manage personal information, contact details, and notification preferences.",
};

export default function ProfilePage() {
  return <ProfileClient />;
}
