import { LoginClient } from "./LoginClient";

export const metadata = {
  title: "Login",
  description: "Access your Horizon Booking account.",
};

export default function LoginPage() {
  return <LoginClient />;
}
